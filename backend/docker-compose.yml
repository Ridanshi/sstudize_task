app:
  build: .
  container_name: auth-backend
  ports:
    - "5000:5000"
  env_file:
    - .env
  depends_on:
    mongo:
      condition: service_healthy
  volumes:
    - .:/app
    - /app/node_modules
  networks:
    - auth-network
  restart: unless-stopped
